import {Text, View} from "react-native";
import styles from "../../styles";

export default function Cart() {
    return (
        <View>
            <Text style={styles.testTxt}>Cart screen</Text>
        </View>
    );
}