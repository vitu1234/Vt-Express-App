import {Text, View} from "react-native";
import styles from "../../styles";

export default function Wishlist() {
    return (
        <View>
            <Text style={styles.testTxt}>Wishlist screen</Text>
        </View>
    );
}